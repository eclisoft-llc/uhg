CREATE TABLE IF NOT EXISTS COMPACT.DIM_COB_CARRIER_CODE (
	COB_CARRIER_CODE_DIM_ID INTEGER NOT NULL CONSTRAINT PK_F_DIM_COB_CARRIER_CODE_DIM_ID PRIMARY KEY,
	COMPANY_DIM_ID INTEGER NOT NULL,
	COB_CARRIER_CODE VARCHAR(15) NOT NULL,
	SHORT_NAME VARCHAR(15) NOT NULL,
	FULL_NAME_1 VARCHAR(60),
	FULL_NAME_2 VARCHAR(30),
	ADDRESS_LINE_1 VARCHAR(40),
	ADDRESS_LINE_2 VARCHAR(40),
	CITY VARCHAR(30),
	STATE VARCHAR(6),
	ZIP_CODE VARCHAR(15),
	COUNTY VARCHAR(5),
	CONTACT_NAME VARCHAR(40),
	CONTACT_TITLE VARCHAR(5),
	CARRIER_TYPE VARCHAR(1),
	PHONE_NUMBER VARCHAR(20),
	EXTENSION VARCHAR(6),
	FAX_NUMBER VARCHAR(20),
	CONVERTED_FROM_DIM_ID INTEGER NOT NULL,
	DW_INSERT_DATETIME DATETIME,
	DW_UPDATE_DATETIME DATETIME,
	SF_INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
	SF_UPDATE_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
	SF_PROCESS_ID VARCHAR(36),
	CONSTRAINT UK_F_DIM_COB_CARRIER_CODE_ID_CODE_CONVERTED UNIQUE (COMPANY_DIM_ID,COB_CARRIER_CODE,CONVERTED_FROM_DIM_ID)
);
