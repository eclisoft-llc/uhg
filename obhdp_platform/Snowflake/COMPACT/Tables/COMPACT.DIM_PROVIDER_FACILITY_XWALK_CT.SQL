CREATE TABLE IF NOT EXISTS COMPACT.DIM_PROVIDER_FACILITY_XWALK (
PROV_DIM_ID NUMBER NOT NULL,
PROV_FACILITY_DIM_ID NUMBER,
EFFECTIVE_DATE DATE,
TERM_DATE DATE,
DW_INSERT_DATETIME DATE,
DW_UPDATE_DATETIME DATE,
SF_INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
SF_UPDATE_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
SF_PROCESS_ID VARCHAR(36),
CONSTRAINT UK_F_DIM_PROVIDER_FACILITY_XWALK UNIQUE (PROV_DIM_ID, PROV_FACILITY_DIM_ID, EFFECTIVE_DATE),
CONSTRAINT DIM_PROVIDER_FACILITY_XWALK_DIM_PROV_FAC FOREIGN KEY (PROV_FACILITY_DIM_ID) REFERENCES COMPACT.DIM_PROVIDER_FACILITY(PROV_FACILITY_DIM_ID),
CONSTRAINT DIM_PROVIDER_FACILITY_XWALK_DIM_PROV FOREIGN KEY (PROV_DIM_ID) REFERENCES COMPACT.DIM_PROVIDER(PROV_DIM_ID)
);
