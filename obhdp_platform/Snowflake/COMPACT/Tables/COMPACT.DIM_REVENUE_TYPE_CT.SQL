CREATE TABLE IF NOT EXISTS COMPACT.DIM_REVENUE_TYPE (
REVENUE_TYPE_DIM_ID NUMBER NOT NULL CONSTRAINT PK_F_DIM_REVENUE_TYPE_DIM_ID PRIMARY KEY,
REVENUE_TYPE VARCHAR(15) NOT NULL,
REVENUE_TYPE_DESC VARCHAR(40),
DW_INSERT_DATETIME DATE NOT NULL,
DW_UPDATE_DATETIME DATE NOT NULL,
SF_INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
SF_UPDATE_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
SF_PROCESS_ID VARCHAR(36),
CONSTRAINT UK_F_DIM_REVENUE_TYPE_REVENUE_TYPE UNIQUE (REVENUE_TYPE)

);
