CREATE TABLE IF NOT EXISTS COMPACT.LOB_BENF_PLAN (
	LOB_BEN_ID NUMBER NOT NULL CONSTRAINT PK_LOB_BENF_ID PRIMARY KEY,
	LOB_ID NUMBER  NOT NULL,
	BENEFIT_PLAN_ID NUMBER  NOT NULL,
	START_DATE DATE,
	END_DATE DATE,
	HAS_EXPIRED NUMBER,
	IS_ACTIVE NUMBER,
	CREATED_ON DATETIME,
	DELETED_BY NUMBER,
	DELETED_ON DATETIME,
	CREATED_BY NUMBER,
	UPDATED_BY NUMBER,
	UPDATED_ON DATETIME,
	DW_INSERT_DATETIME DATETIME,
	DW_UPDATE_DATETIME DATETIME,
    SF_INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
    SF_UPDATE_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP,
    SF_PROCESS_ID VARCHAR(36)
);