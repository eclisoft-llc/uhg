CREATE TABLE IF NOT EXISTS COMPACT.CRR_MEMGROUPADDRESS (   BTCH_LOAD_DT DATE NOT NULL,
   ADDRESS1 VARCHAR(55),
   ADDRESS2 VARCHAR(55),
   ADDRESS3 VARCHAR(55),
   ADDRESS4 VARCHAR(55),
   ADDRESS5 VARCHAR(55),
   ADDRESSTYPE VARCHAR(4),
   CDC_FLAG VARCHAR(2),
   CDC_TS TIMESTAMP_NTZ(9),
   CHANGEDATETIME TIMESTAMP_NTZ(9),
   CHANGEUSERID VARCHAR(20),
   CITY VARCHAR(50),
   COUNTRY VARCHAR(3),
   CREATEDATETIME TIMESTAMP_NTZ(9),
   CREATEUSERID VARCHAR(20),
   EFFECTIVEDATE DATE,
   EXPIRATIONDATE DATE,
   MEMGROUPADDRESSID NUMBER(38,0) NOT NULL,
   MEMGROUPID NUMBER(38,0),
   POSTALCODE VARCHAR(20),
   STATE VARCHAR(2),
   UPDATEVERSION NUMBER(38,0),
   IS_ACTIVE VARCHAR(1) NOT NULL DEFAULT 'Y',   REC_CRT_DTTM TIMESTAMP_NTZ(9) NOT NULL,
   REC_UPDT_DTTM TIMESTAMP_NTZ(9) NOT NULL,
   REC_XTRCT_SRC_CD VARCHAR(4) NOT NULL,
   REC_INPUT_SRC_CD VARCHAR(4) NOT NULL,
   SF_INSERT_DATETIME TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),

   SF_UPDATE_DATETIME TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),

   SF_PROCESS_ID VARCHAR(36),
   constraint PK_CRR_MEMGROUPADDRESS primary key (BTCH_LOAD_DT, MEMGROUPADDRESSID, IS_ACTIVE)  );