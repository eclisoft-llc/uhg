create TABLE IF NOT EXISTS ETL.DBA_CDC_CONTROL (
	SOURCE_DATABASE_NAME VARCHAR(100) NOT NULL,
	TARGET_TABLE_NAME VARCHAR(100) NOT NULL,
	SF_LAST_EXECUTED TIMESTAMP_NTZ(9) NOT NULL,
	constraint PK_DBA_CDC_CONTROL primary key (SOURCE_DATABASE_NAME, TARGET_TABLE_NAME)
);