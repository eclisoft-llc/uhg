CREATE TABLE IF NOT EXISTS ETL.SMART_DIM_IDENT_TYPES_L (
IDENT_TYPE_DIM_ID VARCHAR(50) NOT NULL CONSTRAINT PK_DIM_IDENT_TYPES_DIM_ID PRIMARY KEY,
IDENT_TYPE_CODE VARCHAR(10) NOT NULL,
IDENT_TYPE_DESC VARCHAR(100) NOT NULL,
DW_INSERT_DATETIME VARCHAR(35),
DW_UPDATE_DATETIME VARCHAR(35),
CONSTRAINT UK_DIM_IDENT_TYPES_TYPE_CODE UNIQUE (IDENT_TYPE_CODE)

);
