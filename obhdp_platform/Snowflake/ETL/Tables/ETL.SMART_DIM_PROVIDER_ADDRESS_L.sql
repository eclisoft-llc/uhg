CREATE TABLE IF NOT EXISTS ETL.SMART_DIM_PROVIDER_ADDRESS_L (
PROV_ADDR_DIM_ID VARCHAR(50) NOT NULL CONSTRAINT PK_DIM_PROVIDER_ADDRESS_DIM_ID PRIMARY KEY,
COMPANY_DIM_ID VARCHAR(50) NOT NULL,
PROV_DIM_ID VARCHAR(50) NOT NULL,
PROV_ADDR_ID  VARCHAR(12) NOT NULL,
PROV_ADDR_SUB_ID  VARCHAR(3) NOT NULL,
EFFECTIVE_DATE VARCHAR(35) NOT NULL,
TERM_DATE VARCHAR(35) NOT NULL,
TERM_REASON_DIM_ID VARCHAR(50) NOT NULL,
ADDRESS_LINE_1  VARCHAR(40),
ADDRESS_LINE_2  VARCHAR(40),
ADDRESS_LINE_3  VARCHAR(40),
CITY  VARCHAR(19),
STATE  VARCHAR(2),
ZIP  VARCHAR(11),
COUNTY  VARCHAR(20),
COUNTRY_CD  VARCHAR(4),
PHONE  VARCHAR(20),
PHONE_EXT  VARCHAR(4),
FAX  VARCHAR(20),
FAX_EXT  VARCHAR(4),
EMAIL  VARCHAR(40),
HD_IND  VARCHAR(1),
SVC_LOC_IND  VARCHAR(1),
MAIL_IND  VARCHAR(1),
PRIM_ADDR_IND  VARCHAR(1),
CONVERTED_FROM_DIM_ID VARCHAR(50) NOT NULL,
DW_INSERT_DATETIME VARCHAR(35) NOT NULL,
DW_UPDATE_DATETIME VARCHAR(35) NOT NULL,
CONSTRAINT UK_DIM_PROVIDER_ADDRESS_COMP_PROV_ADDR UNIQUE (COMPANY_DIM_ID,PROV_DIM_ID,PROV_ADDR_ID,PROV_ADDR_SUB_ID,EFFECTIVE_DATE)
);

