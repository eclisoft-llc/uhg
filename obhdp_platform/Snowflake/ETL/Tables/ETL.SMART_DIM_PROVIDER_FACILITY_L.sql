CREATE TABLE IF NOT EXISTS ETL.SMART_DIM_PROVIDER_FACILITY_L (
	PROV_FACILITY_DIM_ID VARCHAR(50) NOT NULL CONSTRAINT PK_DIM_PROVIDER_FACILITY_DIM_ID PRIMARY KEY,
	PROV_FACILITY_ID VARCHAR(20) CONSTRAINT UK_DIM_PROVIDER_FACILITY_PROV_FACILITY_ID UNIQUE,
	PROV_FACILITY_NAME VARCHAR(80),
	PROV_FACILITY_NAME_ALIAS VARCHAR(80),
	SYSTEM VARCHAR(80),
	NATIONAL_SYSTEM VARCHAR(80),
	REGION VARCHAR(20),
	MARKET VARCHAR(30),
	SUB_MARKET VARCHAR(40),
	UNET_MARKET VARCHAR(40),
	STATE VARCHAR(2),
	COUNTY VARCHAR(30),
	ZIP VARCHAR(11),
	MPIN VARCHAR(40),
	TIN VARCHAR(15),
	DUPLICATE VARCHAR(15),
	CLOSED VARCHAR(30),
	BLOCKED VARCHAR(10),
	DW_INSERT_DATETIME VARCHAR(35),
	DW_UPDATE_DATETIME VARCHAR(35)
);
