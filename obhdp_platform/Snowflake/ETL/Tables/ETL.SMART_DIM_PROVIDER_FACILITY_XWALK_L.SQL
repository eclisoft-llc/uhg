CREATE TABLE IF NOT EXISTS ETL.SMART_DIM_PROVIDER_FACILITY_XWALK_L (
PROV_DIM_ID VARCHAR(50) NOT NULL,
PROV_FACILITY_DIM_ID VARCHAR(50),
EFFECTIVE_DATE VARCHAR(35),
TERM_DATE VARCHAR(35),
DW_INSERT_DATETIME VARCHAR(35),
DW_UPDATE_DATETIME VARCHAR(35),
CONSTRAINT UK_DIM_PROVIDER_FACILITY_XWALK UNIQUE (PROV_DIM_ID, PROV_FACILITY_DIM_ID, EFFECTIVE_DATE)
);
