CREATE TABLE IF NOT EXISTS ETL.SMART_DIM_REVENUE_TYPE_L (
REVENUE_TYPE_DIM_ID VARCHAR(50) NOT NULL CONSTRAINT PK_DIM_REVENUE_TYPE_DIM_ID PRIMARY KEY,
REVENUE_TYPE VARCHAR(15) NOT NULL,
REVENUE_TYPE_DESC VARCHAR(40),
DW_INSERT_DATETIME VARCHAR(35) NOT NULL,
DW_UPDATE_DATETIME VARCHAR(35) NOT NULL,
CONSTRAINT UK_DIM_REVENUE_TYPE_REVENUE_TYPE UNIQUE (REVENUE_TYPE)
);

