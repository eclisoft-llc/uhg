CREATE OR REPLACE VIEW FOUNDATION.IMDM_EDW_INDV_KEY_DEDUP AS
SELECT DISTINCT
   FIRST_VALUE(INDV_SRC_ID) OVER(PARTITION BY INDV_KEY_VAL ORDER BY CHG_DTTM DESC) IMDM_ID,
   INDV_KEY_VAL
FROM COMPACT.IMDM_EDW_INDV_KEY
WHERE LOWER(INDV_KEY_TYP_REF_DESC) = 'facetssystemgeneratedids';
