CREATE OR REPLACE VIEW TCOC.ORGANIZATION_TYPE AS
SELECT
   EPIM_IDENTIFIER EPIM_ID,
   HCP_HCO_INDICATOR PROV_TYP_CD,
   NPI,
   SYSTEM,
   CODE,
   DISPLAY,
   TEXT,
   EPIM_LAST_UPDATED_DTTM,
   AUDIT_CREATE_DATE
FROM ETL.EPIM_ORGANIZATION_TYPE_L;
