CREATE OR REPLACE VIEW TCOC.PRACTITIONER_ROLES AS
SELECT
   EPIM_IDENTIFIER EPIM_ID,
   HCP_HCO_INDICATOR PROV_TYP_CD,
   NPI,
   PRACTITIONER_ROLE_IDENTIFIER,
   PRACTITIONER_IDENTIFIER,
   IDENTIFIER_USE_CODE,
   IDENTIFIER_TYPE_CODE,
   PERIOD_START_DATE,
   PERIOD_END_DATE,
   IDENTIFIER_ACTIVE_INDICATOR,
   ASSIGNER_ORGANIZATION_NAME,
   LOCATION_IDENTIFIER,
   EPIM_LAST_UPDATED_DTTM,
   AUDIT_CREATE_DATE
FROM ETL.EPIM_PRACTITIONER_ROLES_L;
