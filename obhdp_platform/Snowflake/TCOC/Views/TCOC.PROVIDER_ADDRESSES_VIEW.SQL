CREATE OR REPLACE VIEW TCOC.PROVIDER_ADDRESSES AS
SELECT
   EPIM_IDENTIFIER EPIM_ID,
   HCP_HCO_INDICATOR PROV_TYP_CD,
NPI,
   ASSOCIATION_RESOURCE_CODE,
   SOURCE_SYSTEM_CODE,
   ADDRESS_IDENTIFIER,
   ADDRESSTYPE_CODE,
   ADDRESS_USE_CODE,
   ADDRESS_USETYPE_CODE,
   ADDRESS_USETYPE_CODE_TEXT,
   LINE_TEXT,
   RANK,
CITY_NAME,
DISTRICT_NAME,
STATE_CODE,
POSTAL_CODE,
   LATITUDE,
   LONGITUDE,
   EPIM_LAST_UPDATED_DTTM,
   AUDIT_CREATE_DATE
FROM ETL.EPIM_PROVIDER_ADDRESSES_L;
