CREATE OR REPLACE VIEW TCOC.PROVIDER_IDENTIFIER_BY_NPI AS
SELECT
   EPIM_IDENTIFIER EPIM_ID,
NPI,
   HCP_HCO_INDICATOR PROV_TYP_CD,
   IDENTIFIER,
   IDENTIFIER_TYPE_CODE,
   IDENTIFIER_SOURCE_SYSTEM_CODE,
   IDENTIFIER_TYPE_CODE_TEXT,
   IDENTIFIER_USE_CODE,
   IDENTIFIER_CHARACTERISTIC_NAME,
   IDENTIFIER_CHARACTERISTIC_VALUE,
   PERIOD_START_DATE,
   PERIOD_END_DATE,
   ASSIGNER_ORGANIZATION_NAME,
   ASSIGNER_ORGANIZATION_IDENTIFIER,
   PROVIDER_ACTIVE_INDICATOR,
   EPIM_LAST_UPDATED_DTTM,
   AUDIT_CREATE_DATE
FROM ETL.EPIM_PROVIDER_IDENTIFIER_BY_NPI_L;
