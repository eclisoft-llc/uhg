CREATE OR REPLACE VIEW TCOC.PROVIDER_QUALIFICATION AS
SELECT
   EPIM_IDENTIFIER EPIM_ID,
   HCP_HCO_INDICATOR PROV_TYP_CD,
   NPI,
   QUALIFICATION_CODE,
   QUALIFICATION_CODE_TEXT,
   EPIM_LAST_UPDATED_DTTM,
   AUDIT_CREATE_DATE
FROM ETL.EPIM_PROVIDER_QUALIFICATION_L;
